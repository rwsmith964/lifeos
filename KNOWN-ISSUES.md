# KNOWN-ISSUES.md

Running log distinguishing "not done yet" from "won't do." Updated as work continues. Superseded entries from round 1 are removed once verified closed — git history has the record if needed.

---

## Fixed and verified live this pass (round 2, D-032)

Duplicate-interest crash (now an upsert, no error at all); duplicate gift-budget crash (same fix, same latent bug, not separately reported); `app/(app)/error.tsx` + `app/global-error.tsx` boundaries; every leaked raw string listed in the brief's 1.3 table plus the capture route's `error.message` interpolation; cadence status now reads real last-contact instead of a column nothing ever wrote to; future birthdates rejected server-side, `estimateAgeYears` floors at null; "Get gift ideas" first-click-does-nothing (missing date default); AI buttons now disabled-with-tooltip via `/api/health` instead of submitting into a guaranteed failure; back link from person detail to `/people`; activity-title casing (was a `capitalize` CSS class on the activities list, not data corruption — removed; seed data ("golf", "fishing", etc.) now renders lowercase as actually stored, which is a minor cosmetic step down for the demo data but is the correct fix for real user input).

## Fixed and verified live this pass (round 2, D-033 — Phase 2, custody)

Migration `20260820000019_custody_schedules.sql` applied live. Multi-day custody blocks now render on every day they span, not just the start date (was actually two bugs: a query that never fetched blocks starting before the visible window, plus a calendar page that only indexed the start day — both fixed). Every custody item on the calendar now shows who-with-whom (`"Emma Smith with Jen"`), not just a bare label. Handover time is a real field end to end (one-off form and the new schedule materializer both use it) — the old hardcoded 5pm is gone. `/calendar/custody` is a new schedule hub (list, create, delete); `/calendar/custody/new` is a recurrence editor (named-preset or fully custom cycle, with a live 14-day preview and gap warnings before saving); the calendar gained a `view=custody` toggle with a per-child color legend. The responsible-parent picker (both the one-off form and the schedule editor) now excludes grandparents, offering only `self`/`co_parent`/`spouse`/`partner`. See D-033 for the full design and the live verification steps run against production.

## Fixed and verified live this pass (round 2, D-034 — AI features now actually functional)

`ANTHROPIC_API_KEY` is set in Vercel. Getting a real key live immediately surfaced two previously-invisible RLS/wrong-client bugs (the health-gating from D-032 could only prove the *absence* of a key was handled gracefully, never that a *present* key actually worked) — both fixed, see D-034: `ai_usage_log` writes (used by all three AI features) and `weekend_plans` writes were both being attempted with the request-scoped user client against tables that, by design, only a service-role client can write. All three AI features — gift suggestions, weekend planning, Quick Capture — are now live-verified working end to end in production, including a full Quick Capture clarifying-question round trip.

## Fixed and verified live this pass (round 3, D-035 — onboarding was completely broken for every new signup)

Creating a household during onboarding failed 100% of the time with an RLS error on `households` — not a policy bug but a `RETURNING`-vs-SELECT-policy chicken-and-egg problem (the user isn't a household member yet at the exact moment the household row is being returned to the client). Fixed with a `SECURITY DEFINER` bootstrap function (`create_household_with_owner`) that does both inserts atomically, matching this schema's existing bypass-RLS-safely pattern. Live-verified end to end with a real account that had never completed onboarding: household creation, redirect to the home dashboard, and the self person record all confirmed working. See D-035.

## Fixed and verified live this pass (round 3, D-036 — delete confirmation everywhere)

Every instant, no-confirm delete flagged in 1.5 (activities, gift history, interests, gift budgets, calendar events/custody blocks) plus person archiving now uses the same two-click "click → Confirm delete → click again" pattern already proven for custody schedules, generalized into a shared hook/component (`lib/hooks/use-confirm-delete.ts`, `components/ui/confirm-delete-button.tsx`). The seven underlying server actions this touched also gained the try/catch + `friendlyMutationError` handling they were missing (a repeat of the D-032 gap, just never exercised for these). See D-036.

## Fixed and verified this pass (round 3, D-037 — field-level error placement, remaining forms)

Interests, contact cadence, settings, and activities now place validation errors under the specific field they concern and clear them on edit, closing out the last of 1.3 (gift budgets and calendar event start/end already had this from D-032). Activities — the one form here with more than two plausibly-invalid fields — needed a different mechanism than text-matching: the create-activity API route now tags its error response with the zod issue's own field path, and the shared `useFormPost` hook carries that through as `errorField`, available to any future multi-field form using that hook. Verified via `pnpm typecheck && pnpm lint && pnpm test && pnpm build` (244/244 tests). See D-037.

## Fixed this pass (round 3, D-038 — bugs found while live-verifying D-037, plus two Phase 3 quick wins)

Live verification of D-037 surfaced three real validation gaps, all fixed: a whitespace-only interest could be saved as a permanently blank, unremovable chip (schema checked length *before* trimming); the activities form's duration field had no real server-side minimum despite the UI implying one ("5 min" could be saved with zero error, anywhere); and fixing a field-level error on the activities form left a stale duplicate generic-error banner behind at the bottom of the form. Also picked up two Phase-3 items while in the area: the people list no longer shows the same name twice when a person has no nickname, and the list now sorts by the name it actually displays (nickname when set) instead of the hidden `full_name` DB column. All five checks (interest, duration, banner, people-list x2) plus the calendar-chip label fix were live-verified on production. See D-038.

## Fixed this pass (round 3, D-039 — calendar Add link now prefills + redirects back to the right day)

The day view's "Add" link already built `/calendar/new?date=YYYY-MM-DD`, but the new-event page never read it — the date field always started blank — and saving always redirected to plain `/calendar` (today), losing whatever day and month you'd actually navigated to. Closes half of the Phase 3 "empty calendar days ... no post-create redirect" item (the other half — every day cell being clickable to select — turned out to already work; every day, not just ones with items, is wrapped in its own Link). Verified via `pnpm typecheck && pnpm lint && pnpm test && pnpm build` (244/244 tests); not yet independently re-verified live in the browser this pass. See D-039.

## Open — from the round 2 brief, not yet started

- **2.x, deferred within Phase 2**: exception-date UI, existing-schedule gap detection, and ICS calendar export are now done (D-043, D-047 — see below). Still open: no edit for an individual materialized custody block beyond delete (editing the parent schedule and letting it re-materialize is the only path).
- **All of Phase 3, 4, 5** — not started, except a few quick wins pulled forward: people list double-name display and sort-by-hidden-field (D-038); raw `kid_activity`-style calendar chips, now humanized via a label map instead of a bare `.replace(/_/g, " ")` (D-038); calendar Add-link date prefill and post-create redirect (D-039 — day cells were already clickable, that part of the brief's description didn't reproduce); Settings timezone free-text field replaced with a real IANA `<select>`, and calendar day-grid navigation now auto-scrolls to the selected day's event list (D-040); custom branded 404 page, and a full forgot-password / reset-password flow (D-041 — live email round-trip not yet independently re-verified); Gifts tab empty-state copy rewritten to name the actual configured horizon, and gift scan horizon is now editable from settings (D-042); custody exception-date UI (add/view/delete single-day overrides on a recurring schedule) plus gap detection surfaced for existing schedules, not just at creation time (D-043); security fix for /reset-password silently accepting any logged-in session instead of requiring an actual clicked reset link (D-044, found during D-042's live verification); PWA manifest, app icons (favicon/apple-touch-icon/maskable), and a minimal installability-only service worker (D-045); dark mode support via next-themes, with a Light/Dark/System toggle on Settings (D-046); custody schedule .ics calendar export (D-047); weekend-plan markdown now actually renders as styled text instead of raw asterisks/dashes, an overdue-contact reminder is suppressed for anyone already on today's custody handover or a shared-activity calendar event, and Brief "People" items link to the matching person page when the name is unambiguous (D-048). Still open: edit-individual-custody-block beyond delete; weather provider + location field; Activities form's five USGS/ODFW/NOAA/lat/lng fields; household membership (Phase 4.1) and everything in Phase 4/5.
- **"Look-ahead" (part of the same backlog line as D-048)** — deliberately NOT implemented. The original detailed wording for this sub-item isn't recoverable from the repo or session history at the point this was picked up, and the rest of that backlog line (markdown rendering, seeing-them-today suppression, People tappability) was concrete enough to build from code inspection alone; this one wasn't. Needs a quick clarifying answer from Richard on what "look-ahead" should mean here (a longer brief event window? a weekly preview section? something else) before it's built.
- **Responsive shell audit (D-049) — done, with one open product question.** A structured audit at mobile/tablet/desktop widths found zero functional bugs (no scrollbars, no truncation, no unclickable controls); the only real bug (the AI-assistant FAB drifting outside the app's column at wide viewports) is fixed. What's still open is a genuine product decision, not a bug: the whole shell is intentionally a narrow ~448px mobile-width column, centered with large unused margins on tablet/desktop. Needs Richard's call on whether LifeOS should ever get a real multi-column desktop layout, or whether "phone-shaped app centered on desktop" is fine indefinitely (it's a legitimate, common pattern for personal apps used mostly on a phone).

## Found during this pass, not in the brief

- **Multiple custody-block rows found for the same Emma/Sep 5–7 span** while cleaning up test data (three rows, not the one the brief described creating). Could be a genuine double-submit bug in the pre-D-031 custody form, or just repeated manual test attempts during round 2's QA pass — not investigated further, since Phase 2 replaces this form outright. Worth keeping an eye on double-submission generally once the new schedule view exists.
- **Gift and notification actions** (`app/(app)/gifts/actions.ts`, `app/(app)/notifications/actions.ts`) weren't individually re-verified live this pass; they use the confirmed-working `startTransition()` direct-call pattern, not the `<form action>` binding, so risk is low but unconfirmed.

## Deferred / won't-do-as-specified (with reason)

None currently — the one item that lived here ("fail the build if `ANTHROPIC_API_KEY` is absent" defaulting to warn-only) is resolved: the key is now confirmed set and working in Vercel, so `REQUIRE_ANTHROPIC_KEY=1` has been set as a Production env var and `scripts/check-ai-config.mjs` now hard-fails the build if the key is ever removed, matching the brief's original ask.
